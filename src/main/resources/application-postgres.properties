database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://dhanpostgre.postgres.database.azure.com/petclinic?SSLMode=require}
spring.datasource.username=${POSTGRES_USER:petclinic}
spring.datasource.password=${POSTGRES_PASS:Mourya@1978}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
